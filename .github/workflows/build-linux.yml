name: Build Linux

on:
  push:
    branches:
      - 'main'
    paths-ignore:
      - 'LICENSE'
      - 'README.md'
      - '.gitignore'
      - '.github/assets/**'
      - '.github/ISSUE_TEMPLATE/**'
      - 'updater/README.md'
  pull_request:
    branches:
      - 'main'
    paths-ignore:
      - 'LICENSE'
      - 'README.md'
      - '.gitignore'
      - '.github/assets/**'
      - '.github/ISSUE_TEMPLATE/**'
      - 'updater/README.md'
  
  workflow_dispatch:
    inputs:
      artifact_upload:
        description: 'Artifact uploading'
        required: false
        type: boolean

env:
  ENV_EXAMPLE_FILE: .env.example
  PACKAGE_FILE: package.json
  BUILD_DIR_TEMP: build_temp
  UPDATER_VERSION: v0.0.1

jobs:
  release:
    if: ${{ startsWith(github.ref, 'refs/heads/main') || startsWith(github.ref, 'refs/heads/dev') }}

    permissions: write-all
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-latest]
        node-version: [14.x]

    env:
      ELECTRON_CACHE: ${{ github.workspace }}/.cache/electron
      ELECTRON_BUILDER_CACHE: ${{ github.workspace }}/.cache/electron-builder

    steps:
      - name: Check out Git repository
        uses: actions/checkout@v1

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}

      - name: Install jq
        run: sudo apt-get install jq -y
      - name: Check jq version
        run: jq --version

      - name: Get yarn cache
        id: yarn-cache
        run: echo "::set-output name=dir::$(yarn cache dir)"

      - name: Cache Node.js modules
        uses: actions/cache@v1
        with:
          path: ${{ steps.yarn-cache.outputs.dir }}
          key: ${{ runner.os }}-yarn-${{ hashFiles(format('{0}{1}', github.workspace, '/yarn.lock')) }}
          restore-keys: |
            ${{ runner.os }}-yarn-

      - name: Install multilib
        run: sudo apt-get -y install gcc-multilib g++-multilib

      - name: Cache Electron
        uses: actions/cache@v1
        with:
          path: ${{ github.workspace }}/.cache/electron
          key: ${{ runner.os }}-electron-cache-${{ hashFiles(format('{0}{1}', github.workspace, '/yarn.lock')) }}
          restore-keys: |
            ${{ runner.os }}-electron-cache-

      - name: Cache Electron-Builder
        uses: actions/cache@v1
        with:
          path: ${{ github.workspace }}/.cache/electron-builder
          key: ${{ runner.os }}-electron-builder-cache-${{ hashFiles(format('{0}{1}', github.workspace, '/yarn.lock')) }}
          restore-keys: |
            ${{ runner.os }}-electron-builder-cache-

      - name: Load variables into the .env file
        shell: bash
        run: |
          source ${{env.ENV_EXAMPLE_FILE}}

          SENTRY_DSN="${{ secrets.SENTRY_DSN }}"
          YANDEX_TRACKING_ID="${{ secrets.YANDEX_TRACKING_ID }}"
          ADS_URLS="${{ secrets.ADS_URLS }}"

          echo "SENTRY_DSN=$SENTRY_DSN" >> .env
          echo "YANDEX_TRACKING_ID=$YANDEX_TRACKING_ID" >> .env
          echo "ADS_URLS=$ADS_URLS" >> .env
          echo "VK_APP_ID=$VK_APP_ID" >> .env
          echo "API_ENDPOINT_URL=$API_ENDPOINT_URL" >> .env
          echo "STATIC_ENDPOINT_URL=$STATIC_ENDPOINT_URL" >> .env
          echo "EXT_API_SERVER=$EXT_API_SERVER" >> .env
          echo "DISCORD_CLIENT_ID=$DISCORD_CLIENT_ID" >> .env
          echo "APP_UPDATE_CHANNEL_ENDPOINT_URL=$APP_UPDATE_CHANNEL_ENDPOINT_URL" >> .env
      
      - name: Get the application version from the "package.json" file
        shell: bash
        run: echo "APP_VERSION=$(cat ${{env.PACKAGE_FILE}} | jq -r '.version')" >> $GITHUB_ENV

      - name: Install dependencies
        shell: bash
        run: |
          sudo chmod +x third-party/updater/download.sh
          ./third-party/updater/download.sh ${{env.UPDATER_VERSION}} linux-x64 && cd third-party/updater/downloads && tar -xzvf linux-x64.tar.gz && cd ../../..
          ./third-party/updater/download.sh ${{env.UPDATER_VERSION}} linux-arm64 && cd third-party/updater/downloads && tar -xzvf linux-arm64.tar.gz && cd ../../..

          yarn
          sudo apt-get -y install \
          libxcb-icccm4 \
          libxcb-render-util0 \
          libxkbcommon-x11-0 \
          libxcb-image0 \
          libxcb-cursor0 \
          libxcb-shape0 \
          libxcb-xkb1 \
          libxcb-keysyms1 \
          libgl1-mesa-glx \
          rpm

      - name: Run build
        shell: bash
        run: |
          mkdir -p ${{env.BUILD_DIR_TEMP}}
          mkdir -p third-party/updater/build/

          updater_path=third-party/updater/build/updater
          sed -i 's/"from": "path\/to\/updater_file"/"from": "third-party\/updater\/build\/updater"/g' ${{env.PACKAGE_FILE}}

          mv third-party/updater/downloads/linux-x64/updater $updater_path
          hash=$(sha512sum $updater_path | cut -d ' ' -f 1)
          sed -i "s/\"updater_checksum_sha512\": \"\",/\"updater_checksum_sha512\": \"$hash\",/" ${{env.PACKAGE_FILE}}
          yarn release:linux
          mv release/*-linux-amd64-*.snap ${{env.BUILD_DIR_TEMP}}/
          mv release/*-linux-x86_64-*.AppImage ${{env.BUILD_DIR_TEMP}}/
          mv release/*-linux-x86_64-*.rpm ${{env.BUILD_DIR_TEMP}}/
          mv release/linux-unpacked ${{env.BUILD_DIR_TEMP}}/

          mv third-party/updater/downloads/linux-arm64/updater $updater_path
          hash=$(sha512sum $updater_path | cut -d ' ' -f 1)
          sed -i "s/\"updater_checksum_sha512\": \"\",/\"updater_checksum_sha512\": \"$hash\",/" ${{env.PACKAGE_FILE}}
          yarn release:linux
          mv release/*-linux-arm64-*.AppImage ${{env.BUILD_DIR_TEMP}}/
          mv release/*-linux-aarch64-*.rpm ${{env.BUILD_DIR_TEMP}}/
          mv release/linux-arm64-unpacked ${{env.BUILD_DIR_TEMP}}/

          rm -rf $updater_path
          sed -i 's/"updater_checksum_sha512": "[^"]*",/"updater_checksum_sha512": "",/' ${{env.PACKAGE_FILE}}
          yarn release:linux
          mv release/*-linux-i386-*.AppImage ${{env.BUILD_DIR_TEMP}}/
          mv release/*-linux-i686-*.rpm ${{env.BUILD_DIR_TEMP}}/
          mv release/linux-ia32-unpacked ${{env.BUILD_DIR_TEMP}}/

      - name: Upload artifact
        if: ${{ inputs.artifact_upload == true }}
        uses: actions/upload-artifact@v3
        with:
          name: Anilibrix Plus
          path: ${{env.BUILD_DIR_TEMP}}/*

      - name: Archiving binary files
        if: ${{ startsWith(github.ref, 'refs/heads/main') }}
        run: |
          cd ${{env.BUILD_DIR_TEMP}}
          zip -r linux-arm64-unpacked.zip linux-arm64-unpacked
          zip -r linux-unpacked.zip linux-unpacked
          zip -r linux-ia32-unpacked.zip linux-ia32-unpacked

          rm -rf linux-arm64-unpacked linux-unpacked linux-ia32-unpacked

      - name: Create Release
        if: ${{ startsWith(github.ref, 'refs/heads/main') }}
        uses: softprops/action-gh-release@v2
        with:
          token: ${{ secrets.GH_TOKEN }}
          tag_name: ${{env.APP_VERSION}}
          files: ${{env.BUILD_DIR_TEMP}}/*